# ============================================
# Django Settings
# ============================================

# Secret Key для Django (ОБЯЗАТЕЛЬНО для production!)
# Сгенерируйте новый ключ командой: python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-w77v&@ua_oe=(jf85dmkg*-c@@qxx3=fj2#b=oqb^ee)e^vl$!

# Debug режим (True для разработки, False для production)
DEBUG=True

# Разрешенные хосты (для production, через запятую)
# Пример: ALLOWED_HOSTS=example.com,www.example.com,localhost
ALLOWED_HOSTS=*


# ============================================
# Database Configuration (PostgreSQL)
# ============================================

DB_NAME=unicrew_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432


# ============================================
# Email Configuration
# ============================================

# Email backend (можно оставить по умолчанию)
EMAIL_BACKEND=backapp.email_backend.CustomSMTPEmailBackend

# SMTP настройки
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Email учетные данные
# Для Gmail нужно использовать App Password, а не обычный пароль
# Инструкция: https://support.google.com/accounts/answer/185833
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password_here
DEFAULT_FROM_EMAIL=your_email@gmail.com


# ============================================
# CORS Configuration (для production)
# ============================================

# Разрешенные источники для CORS (через запятую)
# Пример: CORS_ALLOWED_ORIGINS=https://example.com,https://www.example.com
# Для разработки можно оставить пустым (используется CORS_ALLOW_ALL_ORIGINS=True)
CORS_ALLOWED_ORIGINS=


# ============================================
# Security Settings (для production)
# ============================================

# Перенаправление на HTTPS (True для production)
SECURE_SSL_REDIRECT=False
