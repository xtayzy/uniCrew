# uniCrew

Платформа для поиска команды и участников для проектов в университете.

## Структура проекта

```
uniCrew/
├── back/              # Django Backend
│   ├── backapp/       # Основное приложение
│   ├── unicrewback/   # Настройки Django
│   ├── requirements.txt
│   └── manage.py
├── front/             # React Frontend
│   ├── src/
│   ├── package.json
│   └── vite.config.js
└── docs/              # Документация

```

## Технологии

### Backend
- Django 4.2.24
- Django REST Framework
- PostgreSQL
- JWT Authentication

### Frontend
- React
- Vite
- React Router
- Axios

## Установка и запуск

### Предварительные требования

- Python 3.9+
- Node.js 16+
- PostgreSQL 12+

### Backend

1. Перейдите в папку backend:
```bash
cd back
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте PostgreSQL (см. [docs/POSTGRESQL_SETUP.md](docs/POSTGRESQL_SETUP.md))

5. Примените миграции:
```bash
python manage.py migrate
```

6. Создайте суперпользователя (опционально):
```bash
python manage.py createsuperuser
```

7. Запустите сервер:
```bash
python manage.py runserver
```

Backend будет доступен по адресу: `http://127.0.0.1:8000`

### Frontend

1. Перейдите в папку frontend:
```bash
cd front
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173`

## Переменные окружения

### Backend

Создайте файл `.env` в папке `back/` или установите переменные окружения:

```env
DB_NAME=unicrew_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
```

### Frontend

Настройки API находятся в `front/src/api/apiClient.js`. По умолчанию используется `http://127.0.0.1:8000/api/`

## Документация

- [Деплой с Docker и Dockploy](docs/DOCKER_DEPLOYMENT.md) ⭐ Рекомендуется
- [Деплой на production сервер (ручной)](docs/DEPLOYMENT.md)
- [Настройка PostgreSQL](docs/POSTGRESQL_SETUP.md)
- [Аутентификация](docs/AUTHENTICATION_README.md)
- [Описание проекта](docs/PROJECT_DOCUMENTATION.md)

## Основные функции

- 🔐 Регистрация и авторизация пользователей
- 👥 Просмотр списка пользователей и команд
- 👤 Профили пользователей с навыками и качествами
- 🏢 Создание и управление командами
- 📧 Система уведомлений
- ✅ Управление задачами в командах

## Лицензия

Этот проект создан для образовательных целей.

